# No dependencies need to be installed for this static web project.

# To serve the static files, you can use a simple HTTP server.
# If you have Python 3 installed, you can use its built-in HTTP server.
python3 -m http.server 8000
